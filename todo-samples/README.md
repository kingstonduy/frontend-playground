# React + Vite

```
npm run dev
```

this repo shows the simple todo app using vite + reactjs

feature:
    + input -> store in a list -> show <li>
    + delete an element
    + insert element
    + local storage
    + passing props